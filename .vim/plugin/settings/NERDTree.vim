" Map NERDTree Toggle shortcut
" 配置NERDTree开关快捷键
map <C-N> :NERDTreeToggle<CR>

" Don't close NERDTree windows when open a new file
" 打开新文件时不关闭NERDTree
let NERDTreeQuitOnOpen = 0

" Display bookmarks when NERDTree open
" 默认显示书签
let NERDTreeShowBookmarks = 1

" Set the width of the NERDTree window
" 设置NERDTree窗口宽度
let g:NERDTreeWinSize = 40
